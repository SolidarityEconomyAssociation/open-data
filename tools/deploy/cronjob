#!/bin/bash

SCRIPT=${BASH_SOURCE[0]}
REPO_DIR=$(readlink -f ${SCRIPT%/*}/../..)
SEOD=$REPO_DIR/tools/se_open_data/bin/seod

function process {
    local dir="${1:?directory argument required}"
    (
	cd $REPO_DIR/$dir &&
	    $SEOD download &&
	    $SEOD convert &&
	    $SEOD generate &&
	    sudo -nE $SEOD deploy &&
	    sudo -nE $SEOD triplestore
    )
}

for dir in oxford newbridge mersey-green dotcoop mutual-aid; do
    echo "PROCESSING $dir..."
    process "$dir"
    echo
done

